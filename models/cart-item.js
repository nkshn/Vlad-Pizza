class CartItem {
  constructor(quantity, productName, productSize, productPrice, sum) {
    this.quantity = quantity;
    this.productName = productName;
    this.productSize = productSize;
    this.productPrice = productPrice;
    this.sum = sum;
  }
}

export default CartItem;
